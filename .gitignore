# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

# Bulk research artifacts — never commit (features are ~1 TB, predictions ~27 GB)
features/
predictions/
artifacts/
checkpoints*/
outputs/
results*/
*.safetensors
*.bin
*.pth

# Datasets (download separately; see docs/DATA.md). Anchored to repo root so they don't
# shadow the mtla/data/ package.
/datasets/
/data/

# Audio cascade (AudioSet / Audio Flamingo 3), not part of the unified pipeline yet
/cascade/

# Attention/feature shards anywhere — EXCEPT the small committed demo fixture
*.pt
!fixtures/*.pt

# Generated heatmaps / scratch output from the demo
examples/output/
*.tmp

# OS / editor
.DS_Store
.vscode/
.idea/
*.swp
