# Core scoring + CPU demo. Install with: pip install -r requirements.txt
# (or `pip install -e ".[demo,coco]"` for the same set via pyproject extras).
numpy>=1.24
scikit-learn>=1.2
pyyaml>=6.0
pandas>=2.0
pyarrow>=14.0
torch>=2.1
matplotlib>=3.6
scipy>=1.10
pillow>=9.0
pycocotools>=2.0

# Full GPU reproduction additionally needs (install via `pip install -e ".[extract]"`, or:)
#   transformers==4.57.6  accelerate==1.13.0  torchvision==0.25.0  qwen-vl-utils==0.0.14  decord
#   vllm==0.19.0   (COCO generate stage only)
